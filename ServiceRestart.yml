---
- name: Start Apache HTTPD Service
  hosts: all
  become: yes  # This tells Ansible to use 'sudo' automatically

  tasks:
    - name: Ensure httpd is started and enabled
      service:
        name: httpd
        state: started
        enabled: yes
